[Unit]
Description=Monitoring Script
After=network.target

[Service]
Type=simple
WorkingDirectory=/monitoring
ExecStart=uvicorn main:app --port 6969
Restart=on-failure
Environment=SERVER_RCON={{ lookup('ini', 'rcon.password type=properties file=server.properties') }}

[Install]
WantedBy=multi-user.target