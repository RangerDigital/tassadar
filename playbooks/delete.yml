---
- name: Delete server instance.
  hosts: localhost

  gather_facts: False
  vars:
    hcloud_token: "{{ lookup('env', 'secret.HCLOUD_TOKEN' }}"

  tasks:
    - name: Ensure the server is removed.
      hetzner.hcloud.hcloud_server:
        api_token: "{{ hcloud_token }}"
        name: tassadar
        state: absent
