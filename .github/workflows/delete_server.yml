name: Delete server instance.

on:
  workflow_dispatch:


jobs:
  greet:
    runs-on: ubuntu-latest

    steps:
    # Runs a single command using the runners shell
    - name: Send Greeting
      run: echo "Hello"
